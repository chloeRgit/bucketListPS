{% extends 'base.html.twig' %}

{% block title %}Modify a Wish{% endblock %}

{% block body %}

<h1>Modify a wish</h1>
    {{ form_start(formWish) }}

    {{ form_widget(formWish) }}
    <button type="submit" class="btn btn-secondary">Post</button>
    {{ form_end(formWish) }}
{% endblock %}
